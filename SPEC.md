# Instagram ストーリーズ 自動投稿ツール 仕様書

Claude Code に丸ごと渡すための仕様書。
プロジェクトルートに `SPEC.md` として置き、「SPEC.md を読んで実装して」と指示する。

---

## 1. 目的

店舗の Instagram アカウントに、**1日複数の時間帯（スロット）でストーリーズを自動投稿する**。

スロットごとに以下を独立して設定できること。
- 使う画像フォルダ
- 投稿時刻
- 投稿モード（1枚だけ / フォルダ内全部）

休業日は全スロットを停止する。運用中の操作（画像追加・休業日追加）はすべてスマホから完結すること。

---

## 2. やること / やらないこと

**やる**
- 複数スロットの管理（時間帯ごとに別の画像群）
- スロット単位のモード切替（`rotate` = 日替わり1枚 / `all` = 全部投稿）
- Googleスプレッドシートからの休業日取得（スタッフが編集する窓口）
- 取得失敗時のフォールバック
- 投稿数上限のチェックと超過防止
- アクセストークンの自動リフレッシュ
- 失敗時の通知

**やらない（意図的に対象外）**
- 画像の動的生成（テキスト差し込み不要）
- メンション、リンク、投票などのステッカー（Instagram API が非対応）
- フィード投稿の引用リポスト（API にエンドポイントが存在しない）
- 投稿結果のインサイト取得

---

## 3. 構成

- 実行基盤: **GitHub Actions**
- 言語: **Python 3.11+**
- 依存: `requests` のみ。余計なライブラリを増やさないこと

```
repo/
├── .github/workflows/
│   ├── slot-open.yml        # スロットごとに1ファイル
│   └── slot-night.yml
├── assets/
│   ├── open/                # スロット "open" の画像群
│   │   ├── 01.jpg
│   │   └── 02.jpg
│   └── night/               # スロット "night" の画像群
│       ├── 01.jpg
│       ├── 02.jpg
│       └── 03.jpg
├── config.json              # スロット定義
├── closed_days.txt          # 休業日キャッシュ（自動更新・手で編集しない）
├── post_story.py
└── SPEC.md
```

---

## 4. スロット定義（config.json）

```json
{
  "slots": [
    {
      "name": "open",
      "folder": "assets/open",
      "mode": "rotate",
      "time_jst": "18:00",
      "respect_closed_days": true
    },
    {
      "name": "promo",
      "folder": "assets/promo",
      "mode": "all",
      "time_jst": "12:00",
      "interval_seconds": 30,
      "respect_closed_days": false
    }
  ]
}
```

実際の用途:
- **open** … 営業日のオープン告知。複数枚から日替わりで1枚。休業日は投稿しない
- **promo** … 宣伝用の固定2枚。毎日投稿。**休業日も投稿する**

| キー | 説明 |
|---|---|
| `name` | スロット識別子。CLI と workflow から参照する |
| `folder` | 画像フォルダのパス |
| `mode` | `rotate`（日替わりで1枚）または `all`（フォルダ内全部） |
| `time_jst` | 投稿時刻（JST）。**ドキュメント用。実際の起動は workflow の cron が決める**ので、両者がズレないよう注意 |
| `interval_seconds` | `all` モード時の投稿間隔。省略時は30秒 |
| `respect_closed_days` | `true` なら休業日に投稿しない。`false` なら休業日も投稿する。省略時は `true` |

スロットは**3つ以上に増やせる**こと。配列に足すだけで動く設計にする。

CLI:
```
python post_story.py --slot open
python post_story.py --slot night --dry-run
```

---

## 5. 画像

### 置き方
各スロットのフォルダに入れるだけ。**枚数は自由**で、追加・削除にコード変更は不要。

- 対応拡張子: `.jpg` `.jpeg` `.png`
- 1080×1920（9:16）、8MB以下
- ファイル名は**半角英数字**（日本語やスペースを含む場合はURLエンコードして扱う）
- `_` で始まるファイルは**対象外**（一時的に外したい時、削除せずリネームで済ませるため）

### mode: rotate（1枚だけ）
```
images = sorted(フォルダ内の対象ファイル)
index  = (JSTの今日の日付.toordinal()) % len(images)
選択   = images[index]
```
日付から一意に決まるため**状態ファイル不要**。画像を1枚足せば翌日から自動でローテーションに入る。枚数増減で順番が飛ぶのは仕様として許容。

### mode: all（全部投稿）
- ファイル名の**昇順**に、1枚ずつ順番に投稿する。ストーリーズは投稿順に並ぶため、名前で順序を制御できるようにしておく
- 各投稿の間に `interval_seconds` 待機する（連続投稿は失敗しやすい）
- **投稿前に必ず §8 の上限チェックを行う**

### URL
リポジトリを public にし、raw URL を組み立てる。
```
{IMAGE_BASE_URL}/{folder}/{ファイル名}
例: https://raw.githubusercontent.com/{user}/{repo}/main/assets/open/01.jpg
```
private リポジトリの raw URL は認証が必要で Instagram 側から取得できない。private にしたい場合は画像だけ別の公開ストレージに置くこと。

対象ファイルが0枚の場合は**投稿せずエラー通知**して終了。

---

## 6. 休業日の管理（Googleスプレッドシート）

### 方針
スタッフ全員がGitHubを触るのは非現実的なため、**入力窓口はGoogleスプレッドシート**にする。
リポジトリ内の `closed_days.txt` は**キャッシュ兼バックアップ**として残す（廃止しない）。

```
[スタッフ] → Googleスプレッドシート → [毎回の実行で取得] → closed_days.txt に保存 → 判定
```

### シートの形式

| A: 日付 | B: 種別 | C: メモ |
|---|---|---|
| 2026-08-17 | オープンのみ停止 | |
| 2026-08-20 | 全停止 | 台風 |
| 2026-09-01 | オープンのみ停止 | 棚卸し |

- **B列はプルダウン（データの入力規則）にする。** 手入力させると表記ゆれで事故る
- `オープンのみ停止` … `respect_closed_days: true` のスロットだけ止める
- `全停止` … 全スロットを止める（宣伝スロット含む）
- B列が空欄の場合は `オープンのみ停止` として扱う
- 1行目はヘッダー。読み飛ばす

### 取得方法
シートを「ウェブに公開」してCSVとして取得する。**認証不要**なので追加のAPIキーが要らない。
```
GET {CLOSED_DAYS_CSV_URL}
```
日付だけなので公開しても実害はないが、URLを知る人は閲覧できる点は把握しておくこと。

### 日付パース（重要）
Googleスプレッドシートのエクスポートは環境により表記が揺れる。**最低限これらを全て受け付けること。**
- `2026-08-17`
- `2026/08/17`
- `2026/8/17`

解釈できない行は**警告ログを出してスキップ**し、処理全体は止めない。

### 取得失敗時のフォールバック（最重要）

1. 取得成功 → 内容を `closed_days.txt` に書き出してコミット。これがキャッシュ兼変更履歴になる
2. 取得失敗（ネットワーク断、シート削除、権限変更など） → **`closed_days.txt` の前回内容を使って続行**。同時に通知を飛ばす
3. 取得失敗かつ `closed_days.txt` も無い → **投稿せずに終了し、通知を飛ばす**

3 の判断理由を明記しておく。**休業日に「本日OPEN」を誤って出す方が、営業日に投稿しそこねるより損害が大きい。** 情報が無い時は投稿しない側に倒す。

### 緊急停止
シートのA1セルに `PAUSE` と入力すると、日付に関係なく全スロットを停止する。長期休業や不具合時の緊急ブレーキ。

---

## 7. 投稿処理

**採用するAPI: Instagram API with Instagram Login**（Facebookページ連携が不要な軽量ルート）
ベースURLは `graph.facebook.com` ではなく **`graph.instagram.com`**。

必要なスコープ: `instagram_business_basic`, `instagram_business_content_publish`
APIバージョンは着手時点の最新安定版を公式ドキュメントで確認して使う。

**Step 1: コンテナ作成**
```
POST https://graph.instagram.com/{version}/me/media
  image_url    = <公開画像URL>
  media_type   = STORIES
  access_token = <token>
→ { "id": "<creation_id>" }
```

**Step 2: 公開**
```
POST https://graph.instagram.com/{version}/me/media_publish
  creation_id  = <creation_id>
  access_token = <token>
```

Step 1 と Step 2 の間に **5秒待機**。コンテナ処理完了前に publish するとエラーになる。

---

## 8. 投稿数上限（`all` モードで重要）

Instagram には24時間あたりの API 投稿数上限がある。**Meta のドキュメント内でも 50 と 100 の2つの数字が併記されており、実際の値はアカウントによって異なる。数字をハードコードしないこと。**

`all` モードの実行前に、必ず現在の残量を問い合わせる。
```
GET https://graph.instagram.com/{version}/me/content_publishing_limit
```

- 残量 ≧ 投稿予定枚数 … そのまま実行
- 残量 < 投稿予定枚数 … **入る分だけ投稿し、残りはスキップして通知**（全部失敗させない）
- 残量 0 … 投稿せず通知して終了

上限は**カレンダー日ではなく24時間の移動窓**。深夜0時にリセットされるわけではない点に注意。

---

## 9. タイムゾーンと実行タイミング

- GitHub Actions の cron は **UTC**
- 「今日」の判定は必ず **JST（Asia/Tokyo）** で算出する。`datetime.now()` をそのまま使わない
- 例: 18:00 JST → `0 9 * * *` / 22:00 JST → `0 13 * * *`
- **GitHub Actions の scheduled workflow は混雑時に数分〜15分程度遅延することがある。** 分単位の正確さが要る場合は cron を早めに設定するか、遅延を許容する

workflow はスロットごとに1ファイル作る。中身はほぼ同じで `--slot` の値と cron だけが違う。重複を嫌って1ファイルにまとめるより、**分けた方が事故が起きにくい**。各ファイルに `workflow_dispatch` を入れて手動実行できるようにする。

---

## 10. アクセストークン

- 長期トークンの有効期限は **60日**
- **週1回のリフレッシュジョブ**を用意する
  ```
  GET https://graph.instagram.com/refresh_access_token
    ?grant_type=ig_refresh_token&access_token=<現在のtoken>
  ```
- 新しいトークンは `gh secret set` で GitHub Secrets に書き戻す（`repo` スコープの PAT が必要）
- 書き戻しに失敗したら**必ず通知**。無言で失敗すると60日後に全部止まる
- トークンは最低1回使われていないとリフレッシュできない。長期休業明けは注意

---

## 11. Secrets

| 名前 | 内容 |
|---|---|
| `IG_ACCESS_TOKEN` | 長期アクセストークン（60日） |
| `IMAGE_BASE_URL` | raw URL のベース（末尾スラッシュなし） |
| `GH_PAT` | Secrets 更新用（repo スコープ） |
| `NOTIFY_WEBHOOK` | 失敗通知先（Discord / Slack） |
| `CLOSED_DAYS_CSV_URL` | 休業日シートの公開CSV URL |

`me/` エンドポイントを使うためユーザーIDは不要。

---

## 12. 事前準備（実装前に完了させる）

1. Instagram アカウントを**プロアカウント（ビジネス）**に切り替える
2. https://developers.facebook.com で開発者登録
3. アプリを新規作成し、プロダクトに **Instagram** を追加。ログイン方式は **Instagram Business Login**
4. スコープ `instagram_business_basic` / `instagram_business_content_publish` を設定
5. 自分のアカウントを連携し、短期トークンを取得 → 長期トークンに交換
6. **App Review は不要。** 自分が所有・管理するアカウントのみを対象とする場合は Standard Access で足りる

---

## 13. エラー処理

- API 呼び出しは**最大3回リトライ**、指数バックオフ（5秒 → 15秒 → 45秒）
- **`all` モードの途中失敗は、そこで止めずに残りを続行する。** 全件終了後にまとめて通知し、exit code 1 で終了
- 通知には「スロット名」「何枚中何枚成功したか」「失敗したファイル名」「HTTPステータス」「レスポンスbody」を含める
- **ログにアクセストークンを絶対に出力しない**。マスクすること

---

## 14. 動作確認用

### `--dry-run`
実際には投稿せず、以下を出力。
- 投稿するか / しないか（しない場合の理由：単日・期間・曜日・PAUSE のどれか）
- 投稿予定の画像ファイル名とURL（`all` なら全件、`rotate` なら1件）

### `--check`
**今後14日間の予定を全スロット分、一覧表示する。** 不定休運用では、休業日を書き足した後にその場で効果を確認できることが重要。

```
2026-08-16 (土)  open:投稿 02.jpg   night:投稿 3枚
2026-08-17 (日)  open:投稿 03.jpg   night:投稿 3枚
2026-08-18 (月)  open:休業 ← closed_days.txt:12   promo:投稿 2枚
2026-08-19 (火)  open:休業 ← every-tue            promo:投稿 2枚
```

休業日でも `respect_closed_days: false` のスロットは投稿予定として表示されること。

休業行には**どの記述が効いたか**を必ず表示すること。書き間違いに気づけるようにするため。

---

## 15. 受け入れ基準

1. `--check` が今後14日分を全スロット分、休業理由つきで正しく表示する
2. `--dry-run` が休業日／営業日それぞれで正しく判定を出す
3. `config.json` にスロットを1つ足すだけで、3スロット目が動く
4. `rotate` スロットのフォルダに画像を1枚追加しても、コード変更なしでローテーションに入る
5. `all` スロットのフォルダに画像を1枚追加すると、投稿枚数がそのまま1枚増える
6. `all` モードでファイル名昇順どおりに投稿される
7. 投稿数上限が不足しているとき、入る分だけ投稿して通知が飛ぶ
8. `all` モードの途中で1枚失敗しても、残りが投稿され、失敗分が通知される
9. 対象フォルダが空のときはエラー通知が飛ぶ
10. シートに `オープンのみ停止` を追加すると、openスロットだけが止まりpromoは投稿される
11. シートに `全停止` を追加すると、両スロットとも止まる
12. A1に `PAUSE` を入れると全スロットが止まる
13. 実行後、シートの内容が `closed_days.txt` に反映されコミットされている
14. CSV取得を故意に失敗させると、`closed_days.txt` の内容で動作し通知が飛ぶ
15. CSV取得失敗かつ `closed_days.txt` 不在のとき、投稿されず通知が飛ぶ
16. `2026/8/17` 形式の日付が正しく解釈される
17. 壊れた行（例: `2026-13-45`）があっても処理が止まらず、警告だけ出る
18. トークンを故意に壊すと通知が飛ぶ
19. ログにトークンが一切現れない
