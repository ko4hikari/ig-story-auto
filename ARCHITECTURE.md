# アーキテクチャ・技術選定メモ

このドキュメントは「なぜこの構成にしたか」「運用コストがどこにもかからない設計になっているか」を、セッションが切れても後から追えるように残すもの。
実装の詳細は `SPEC.md`、運用手順は `README.md`、作業の進捗は `PROGRESS.md` を参照。

---

## 1. 前提: 月額固定費ゼロを狙う

このツールはすべて **無料枠の範囲で完結する構成** にしてある。有料サーバー・有料SaaSを一切使わない。

```
[スタッフのスマホ]
   │ 画像を追加          │ 休業日を編集
   ▼                     ▼
[GitHubリポジトリ(public)]   [Googleスプレッドシート]
   │  raw URLで画像配信         │ 「ウェブに公開」でCSV配信
   │                            │
   ▼                            │
[GitHub Actions (cron)] ────取得┘
   │
   ├─ Instagram API (graph.instagram.com) へ投稿
   └─ 失敗時のみ Discord/Slack Webhook で通知
```

常時稼働のサーバー・VPS・データベースを一切持たない。GitHub Actions の scheduled workflow が定刻に起動して仕事をし、終わったら消えるだけの構成。

---

## 2. 使用技術と選定理由（コスト観点）

| 要素 | 選定 | 選んだ理由 | コスト |
|---|---|---|---|
| 実行基盤 | **GitHub Actions** | public リポジトリなら実行時間が無制限・無料。cron機能を内蔵していて、追加の実行環境を用意しなくていい。`workflow_dispatch` でスマホからも手動実行できる | $0 |
| 画像配信 | **raw.githubusercontent.com** | リポジトリを public にして画像を置くだけで、認証不要の公開URLがそのまま手に入る。S3やCloudinaryのような画像ホスティング契約が不要 | $0（ただしリポジトリを public にする必要あり。§4参照） |
| 休業日の入力窓口 | **Googleスプレッドシート「ウェブに公開」** | 非エンジニアのスタッフでも普段使っているスプレッドシートで完結。APIキーなしで CSV を GET できるので、Google Cloud のプロジェクト作成・認証設定が丸ごと不要 | $0 |
| 言語・依存ライブラリ | **Python 3.11 + `requests` のみ** | GitHub Actions のランナーに Python はプリインストール済み。依存を増やさないので `pip install` の待ち時間や壊れる要因を最小化 | $0 |
| 投稿API | **Instagram API with Instagram Login**（`graph.instagram.com`） | Facebookページ連携が必須の旧ルートではなく、Instagramアカウント単体で完結する軽量ルート。自分が所有・管理するアカウントのみが対象なら **App Review が不要**（Standard Access） | $0（Meta 開発者登録も無料） |
| 障害通知 | **Discord / Slack の Incoming Webhook** | 監視SaaS（PagerDuty等）を使わず、普段見ているチャットにそのまま流す | $0（既存の無料ワークスペースの範囲） |
| トークン保管・更新 | **GitHub Secrets** + 週1回の自動リフレッシュ workflow | Vault等の秘密情報管理サービスを使わず、GitHubの標準機能内で完結 | $0 |

**運用コスト: 月額 $0。** 発生する費用は「独自ドメインを使いたい」「画像を非公開にしたい」など要件が増えたときだけ。

---

## 3. なぜこの構成なのか（設計判断の要点）

- **サーバーを持たない**: 常時起動のプロセスがあるとその分の課金が発生する。GitHub Actions の scheduled workflow はイベント駆動（cron起動）なので、起動していない時間の費用が存在しない
- **画像ホスティングを別サービスにしない**: リポジトリを public にすれば raw URL がそのまま CDN 代わりになる。画像の追加・削除も Git 操作（＝スマホの GitHub アプリからのアップロード）で完結し、別管理画面を覚える必要がない
- **休業日入力を非エンジニア向けに分離**: GitHubを触れないスタッフのために、入力窓口だけ Google スプレッドシートに切り出した。取得は「ウェブに公開」のCSVで済むため、Google Sheets API の有効化・サービスアカウント発行のような開発者向け作業が不要
- **依存ライブラリを増やさない**: `requests` 一本に絞ることで、Actions 実行のたびに発生する `pip install` の時間・失敗リスクを抑えている

---

## 4. 唯一お金・制約がからむ可能性がある点

- **リポジトリを public にする必要がある**: private リポジトリの raw URL は認証が必須になり、Instagram 側（外部）から画像を取得できない。画像を非公開にしたい場合は、画像だけ別の公開ストレージ（S3 + CloudFront など）に出す必要があり、そこで初めて費用が発生する。現状はこれを避けるために public のまま運用している
- **GitHub Actions の無料枠は public リポジトリが前提**: private にすると Free プランでは月2,000分等の実行時間上限が付く。public のままなら該当しない
- **Google スプレッドシートのCSV公開は「URLを知っていれば誰でも閲覧できる」**: 中身は日付とメモだけなので実害は小さいが、機密情報を書かないよう運用上の注意が必要（`SPEC.md` §6 にも記載）

---

## 5. 今後スロットや画像を増やしても費用は変わらない

- スロット追加（`config.json` に追記 + `assets/` にフォルダ追加 + workflow ファイルをコピー）は全てリポジトリ内で完結する
- 画像を増やしても、GitHub の public リポジトリのストレージ・帯域幅は個人利用の範囲で実質無料
- Instagram Graph API の呼び出し自体にも費用はかからない（投稿数の上限はあるが課金ではない。`SPEC.md` §8）
